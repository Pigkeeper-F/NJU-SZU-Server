# 业务流程图 · 流程智能体（两阶段主流程 + 能力节点）

由业务说明整理出的可编辑流程图（draw.io 源文件 + 渲染产物），用于设计智能体的能力节点。

## 图里有什么

- **阶段一 · 研究写作过程（6 步）**：① 文献调研 → ② 科学问题的凝练与提取 → ③ 数据获取、清洗处理 → ④ 模型的选择与运行 → ⑤ 图表的绘制 → ⑥ 文章的撰写和修改
- **阶段二 · 投稿—审稿—发表过程（3 步）**：⑦ 投稿前的全流程校验与投稿 → ⑧ 回复评审意见与最终校验 → ⑨ 文章接收后校稿阶段的校验
- **能力节点细化**（三个关键模块的内部流程，含人工审核判断与「未通过」回退环路）
  - A 数据获取与清洗 → 计算（方法不变、改 input 得结果）
  - B 新模块接入：数据对齐（对齐文档 → 人工审核 → 写成代码 → 运行 → 人工审核 → 自动定位：数据/对齐/运行代码）
  - C 参数数据与文献数据挖掘（威源的完整流程）

图例：蓝框 = 智能体自动执行；橙菱形 = 人工审核/判断；绿框 = 输出；灰虚线 = 未通过回退路径。

## 文件

| 文件 | 说明 |
|---|---|
| `flow-agent-process.drawio` | **源文件**（draw.io 可直接打开编辑） |
| `build_process_chart.py` | 生成脚本：改文案/布局后重新生成 `.drawio`（零依赖，只用标准库） |
| `render_previews.py` | 预览工具：按内容边界正确裁切预览图 + PDF 文本核对 |
| `render/flow-agent-process.pdf` | **矢量单页**（投稿插图用，文字可选中可搜索） |
| `render/flow-agent-process.drawio.png` | 位图（2x，3315×2554，微信/PPT 直接贴） |
| `versions/v1/` | 初版快照（`.drawio` + `MD5SUMS.txt`），可回滚对比 |

## 编辑与重新导出

```bash
# ① 改内容：编辑 build_process_chart.py 后重新生成源文件
python build_process_chart.py

# ② 导出产物（Windows 桌面版路径示例）
DRAWIO="C:/Users/33477/AppData/Local/Programs/draw.io/draw.io.exe"
"$DRAWIO" -x -f png -e -s 2 -b 10 -o render/flow-agent-process.drawio.png flow-agent-process.drawio
"$DRAWIO" -x -f pdf -e -o render/flow-agent-process.pdf flow-agent-process.drawio

# ③ 预览（含内容边界映射，画图前先看它）
python render_previews.py
```

**两个坑（脚本里已处理，手工导出时注意）**

1. draw.io CLI 的 `-x` 是按**内容边界**裁剪（不是页面边界）：页面坐标 → PNG 像素换算必须先求内容左上角（沿父链累加容器偏移，且只统计 `vertex`——`edge` 的 `mxGeometry` 没有 x/y）。
2. 容器标题这类文本若漏传 `parent`，会全部堆在页面左上角叠成一团；自动生成的 id 前缀别与步骤框 id 撞车（会出现重复 id）。

## 排版提示（给投稿用）

- PDF 出图 **1238×936 pt**，实测字号：正文 **11.5pt**、小字 9.3pt、标题 21.6pt
- 放进 A4 横向正文宽（约 24cm）还会再缩约 0.55 → 正文约 **6.3pt**（多数期刊要求 ≥6pt，属于"能看但偏小"）
- 若目标是**双栏论文的单栏**，建议拆成「主流程图」和「能力节点图」两张分别排版

## 版本记录

| 版本 | 变化 |
|---|---|
| v2（当前） | 全图字号 ×1.35（正文 12→16pt）、菱形放大避免文字溢出、阶段间连接居中、图例加边框、回退环路标签内收 |
| v1 | 初版（见 `versions/v1/`，md5 记录在 `versions/MD5SUMS.txt`） |

## 待业务确认（未擅自改动）

1. **阶段二是否补「评审结果」分支**：现为直线 ⑦→⑧→⑨；现实中评审结果为 接收/小修/大修/拒稿，后两者应打回阶段一 ⑥ 返修再投（加一个菱形 + 一条回 ⑥ 的虚线）。
2. **节点 A 的「结果要人审核」是否独立成菱形**：B、C 都是独立菱形，A 目前写在输出框文字里；统一后 A 容器需加高约 90pt，三个容器要一起调整。
